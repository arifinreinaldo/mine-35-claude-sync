# Index — all rooms

> Flat list of every wiki page, one line each. Keep in sync on `update`.

- [[map]] — the floor plan / intent router (start here)
- [[architecture]] — system shape, entry points, data flow
- [[code_map]] — code path → room (the address book)
- [[conventions]] — patterns, naming, state, gotchas
- _areas/_ — deep per-feature rooms (added on demand)
