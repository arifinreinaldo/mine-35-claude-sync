# Code map (address book)

> Reverse lookup: a changed file → the room(s) that describe it. The `update` op reads this first.
> One row per meaningful path. `Page` = `TBD` until a room exists.

| Code path | Purpose | Page |
|---|---|---|
| `{{ENTRYPOINTS}}` | entry point(s) | [[architecture]] |
| _(seed: one row per top-level dir)_ | | TBD |
